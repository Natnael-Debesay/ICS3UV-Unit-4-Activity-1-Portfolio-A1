/**
 * @author Natnael Debesay
 * @version 1.0.0
 * @date 2025-12-04
 * @fileoverview This program counts down from 100 by 5s.
 */

// variables
let number: number = 0;

// print numbers
for(
  let number=100; number>=0; number -= 5) { 
  console.log(number);
}

console.log("\nDone.");
